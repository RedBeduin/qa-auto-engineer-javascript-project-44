const randomNumberGeneratorX10 = () => Math.round(Math.random() * 100);
const randomNumberGenerator = () => Math.round(Math.random() * 10);

export { randomNumberGeneratorX10, randomNumberGenerator };
